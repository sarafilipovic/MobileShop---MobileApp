<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobileShopMobileApp" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">#fff</item>
    </style>

    <style name="Theme.MobileShopMobileApp" parent="Base.Theme.MobileShopMobileApp" />
</resources>